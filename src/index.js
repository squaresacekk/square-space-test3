import React from 'react';
import ReactDOM from 'react-dom/client';
import BookingTestPage from './BookingTestPage';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <BookingTestPage />
  </React.StrictMode>
);
